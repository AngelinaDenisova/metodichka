#include <iostream>
using namespace std;
int main() {
	int n;
	double s;
	s = 0;
	cout << "n= ";
	cin >> n;
	for (int i = 1; i <= n; i++) {
		s = s + (pow(-1, n) / pow(2, n));
	}
	cout << "s = " << s;
	return 0;
}
