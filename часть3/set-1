#include <iostream>
#include <set>
#include <cmath>
using namespace std;

int main() {
    int n, num;
    set<int> digits;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> num;
        if (num % 10 == 0) {
            while (num > 0) {
                digits.insert(num % 10);
                num /= 10;
            }
        }
    }
    for (int i : digits)
        cout << i << " ";
    return 0;
}
