//найти количество строк, которые начинаются и заканчиваются одной буквой (не работает)
#include <iostream>
#include <string>
#include <fstream>
using namespace std;
int main() {
    int count = 0;
    string s;

    ifstream in("f.txt");
    while(getline(in, s)){
        if (s[0] == s.back()){
            count++;
            }
    }
    cout << count;
    in.close();
    return 0;
}

