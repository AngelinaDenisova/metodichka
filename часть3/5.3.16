#include <iostream>
#include <cmath>
using namespace std;
int main() {
    int n, i = 1;
    double e, s = 0, a = 1;
    cout << "e = ";
    cin >> e;
    while (fabs(a) >= e) {
        s += a;
        i++;
        a = 1/(pow(3, i)+pow(4, i));
    }
    cout << "s = " << s << endl;
    return 0;
}
