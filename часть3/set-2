#include <iostream>
#include <string>
#include <vector>
#include <set>
#include <algorithm>
using namespace std;
int main() {
    int N;
    cin >> N;
    vector<string> strings;
    for (int i = 0; i < N; i++) {
        string s;
        getline(cin, s);
        strings.push_back(s);
    }
    for (int i = 0; i < N; i++) {
        set<char> upper; //множество с прописными буквами
        for (int j = 0; j < strings[i].length(); j++) {
            if (isupper(strings[i][j])) { //если символ - прописная буква
                upper.insert(strings[i][j]); //добавляем ее в upper
            }
        }
        for (char c: strings[i]){
            if (islower(c) && upper.count(toupper(c))==0)
             cout << c << " ";
            }
            cout << endl;
        }
    return 0;
}
